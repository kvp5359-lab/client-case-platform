-- Флаг «дополнительная услуга» (не входит в пакет): визуально отделяется
-- в списке услуг проекта, сводка показывает разбивку «пакет + допы».
-- Аддитивно, дефолт false — существующие строки считаются пакетными.
-- Применена в прод через MCP 2026-07-04.
ALTER TABLE public.project_services
  ADD COLUMN IF NOT EXISTS is_extra boolean NOT NULL DEFAULT false;
