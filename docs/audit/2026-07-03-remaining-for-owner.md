# Аудит — что сделано автономно и что осталось на владельца (2026-07-03)

Документ дополняет [мастер-план](2026-07-03-phase-9-master-plan.md). Ниже: (1) что уже
сделано в этой сессии, (2) что требует твоих действий (деплой, панели, решения).

---

## ✅ Уже сделано (в этой сессии)

### Применено в БОЕВУЮ базу через MCP (ты одобрил) + миграции в репо
- **A1 — закрыт анонимный доступ к inbox-превью.** `REVOKE anon` на 3 RPC
  (`get_inbox_needs_reply/awaiting_reply/muted`) + `get_my/set_my_thread_notify_level`.
  Миграция `20260703120000_revoke_anon_inbox_and_notify_rpcs.sql`.
- **C4 — 16 индексов на непокрытые внешние ключи** (в т.ч. горячие
  `project_messages.sender_participant_id`, `thread_unread_state.thread_id`).
  Применено через `CREATE INDEX CONCURRENTLY` (без блокировки). Миграция
  `20260703120500_index_unindexed_foreign_keys.sql`.

Эти две правки **уже действуют в проде**. Ничего доделывать не нужно.

### Закоммичено локально (НЕ запушено) — доедет до сайта после `git push` + CI
- **B8 — 41 тест** на критичное без покрытия (filterEngine, htmlToTelegramHtml с
  регрессией `<ol start>`, reorderWithinZones). 794 теста всего, зелёные.
- **B10 — онбординг-доки**: `.env.example` + MODULE-переменные, `cp .env.example`,
  Node≥22 (`engines`) — новый разработчик поднимет проект с нуля.
- **D1 — focus-visible в popover** (вернул клавиатурную навигацию, WCAG).
- **D5 — удалены 3 мёртвых экспорта.**
- **B4 — дружелюбные ошибки** на error-страницах + хелпер `getUserFacingErrorMessage`
  (+8 тестов). Сырой SQL/английский больше не показывается пользователю.
- **B7 — 12 нативных `confirm()` → `useConfirmDialog`** (см. статус — если этот пункт
  здесь, значит закоммичен).
- **B5 — мобильные hover-действия** (`opacity-0 group-hover` → `md:opacity-0
  md:group-hover`) — действия видны на тач (если пункт здесь — закоммичен).

### Закоммичено локально, но требует РУЧНОГО ДЕПЛОЯ edge-функций (не через CI)
- **A3/A4 — закрыты частичные IDOR в Google-функциях** (сверка воркспейса):
  `google-sheets-create-brief`, `export-to-drive`, `google-drive-download-file`,
  `google-drive-export-documents`. **Код в репо, НЕ задеплоен.**

---

## ⏳ Осталось на тебя

### 1. Запушить и задеплоить накопленное
- `git push` (после твоего «да») → CI выкатит фронт (B4, B5, B7, B8, B10, D1, D5).
- **Задеплоить 4 Google edge-функции** (A3/A4), лучше глядя в логи (мониторинга нет):
  ```
  supabase functions deploy google-sheets-create-brief --project-ref zjatohckcpiqmxkmfxbs
  supabase functions deploy export-to-drive --project-ref zjatohckcpiqmxkmfxbs
  supabase functions deploy google-drive-download-file --project-ref zjatohckcpiqmxkmfxbs
  supabase functions deploy google-drive-export-documents --project-ref zjatohckcpiqmxkmfxbs
  ```
  Смок: выгрузка документов в Google Drive и создание брифа работают как раньше
  (в СВОём воркспейсе), а чужие id/пути отдают 403.

### 2. Только через панель Supabase (я не могу)
- **A2 🟠 — публичный бакет `docbuilder` (417 документов клиентов).** Сейчас раздаётся
  по публичным URL. Сделать бакет **private** + перевести раздачу на signed URL. ⚠️ Это
  требует ПАРНОЙ правки кода (`analyze-documents`, `generate-block` и фронт, где строятся
  `…/object/public/docbuilder/…`) — иначе просмотр документов сломается. Отдельная сессия.
- **B2 🟠 — бэкапы**: проверить/включить PITR; настроить бэкап Storage и VPS
  (mtproto `.env` + сессии); описать restore-процедуру.
- **B3 🟠 — регион данных**: подтвердить регион Supabase; базовый DPA по сути.

### 3. Требует настройки внешних сервисов
- **A6 🔴 — мониторинг** (сейчас его НЕТ): Sentry/GlitchTip на фронт+edge (задел
  закомментирован в `src/utils/logger.ts`), uptime-пинг домена + mtproto, алерт на
  cron-фейлы и рост `message_send_failures`. Без этого о сбоях узнаёшь от клиента.

### 4. Карантин / требует смок-теста (не делал автономно — риск на живом мессенджере)
- **A5 🟠 — RLS `project_messages_insert`**: ветка `channel='client'` пропускает без
  проверки воркспейса → инъекция сообщения в чужой чат. ⚠️ **Осторожно**: нельзя просто
  «всегда требовать is_internal_member» — клиенты (не-staff) легально вставляют
  client-сообщения. Нужно требовать членство в воркспейсе + доступ к треду, со смок-тестом
  отправки от клиента и сотрудника.
- **B9 🟠 — карантинный хвост**: разобрать накопленные «⏳ ЖДЁТ ДЕПЛОЯ/СМОК» из ledger
  (особенно «удаление файлов» — миграция + edge + смок вместе). Полный чеклист — в
  [phase-7-quarantine.md](2026-07-03-phase-7-quarantine.md), часть А.
- **C10 — остаток G10** (гонка `_botToken` на отправке) + soft-delete/plain-эхо в
  group-бот/Business.

### 5. Крупные инфра-задачи под рост (корзина C, отдельными сессиями)
- **C1 — оптимизация тяжёлых inbox-RPC** (`needs_reply`/`awaiting` 555 мс) +
  пагинация непагинированных сегментных RPC (риск потери при >1000 тредов).
- **C2 — снизить WAL-нагрузку Realtime** (~63% времени БД): урезать publication.
- **C3 — апгрейд инстанса + connection pooler** (сейчас 60 соединений).
- **C7 — squash-миграция** для воспроизводимости (репо не воспроизводит прод: 688
  миграций против 308 файлов).
- **C8 — биллинг + лимиты/квоты.**
- **C9 — mtproto healthcheck + миграции в CI.**

### 6. Не критично (корзина D, по остаточному принципу)
- **D8-хвост** — убрать `anon` ещё с ~77 SECURITY DEFINER предикатов (через PUBLIC,
  нужен `REVOKE PUBLIC` + повторный GRANT + смок; сохранить anon у 4 резолверов ссылок).
- Остальное a11y (aria-label, тач-цели 44px, div→button), скелетоны, терминология,
  мобильный календарь, чистка 177 «unused» индексов, зомби-контейнеры certbot на VPS,
  разовый разгреб 8 старых `pending`-сообщений.

---

## Короткий следующий шаг
1. `git push` (нужно твоё «да») → фронт-правки уедут.
2. Задеплоить 4 Google-функции (команды выше).
3. Поставить мониторинг (A6) — дешевле всего, а закрывает главный слепой зон.
4. Проверить бэкапы/PITR (B2).
Остальное — по мастер-плану, отдельными сессиями.
