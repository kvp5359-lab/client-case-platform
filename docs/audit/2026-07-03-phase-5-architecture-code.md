# Аудит, Фаза 5 — Архитектура и качество кода (2026-07-03)

## Резюме

Кодовая база в хорошем инженерном состоянии: 0 TODO/FIXME, нет `.bak`/`.orig`-файлов, нет неиспользуемых зависимостей, все лицензии permissive (MIT/Apache/ISC/BSD — GPL/AGPL нет, коммерческой продаже ничто не мешает), нарушений слоёв на рантайме нет (импорты из `components` в `services/store/hooks` — только `import type`, 15 шт., мягкий остаток T1), `@ts-ignore` — 0. Дублей значений с рассинхроном не найдено (палитра акцентов и digest-промпт синхронны). Главный долг — **тесты**: 753 кейса покрывают hooks/utils/services, но три критичных по продукту места остались без тестов и все три уже ломались или имеют gotcha (фильтр-движок, HTML-в-Telegram, reorder сайдбара). Второй пункт — **онбординг-документация неполна**: новый разработчик не поднимет проект «из коробки» без личной помощи. Большие файлы — в основном оркестраторы (по списку false-positives не репортятся).

## Находки

| # | Объект | Серьёзность | Проблема | Чем грозит | Решение | Труд |
|---|---|---|---|---|---|---|
| 1 | `src/lib/filters/filterEngine.ts` (`applyFilters`, 431 стр.) | 🔴 | **Нет тестов.** Питает ВСЕ доски, списки, табличные виды. Логика AND/OR, динамические даты (`__today__`, `__last_n_days__`), `__me__`/`__creator__`. | Баг в фильтре → клиент видит НЕ те задачи или чужие, молча (без ошибки). Тихая потеря доверия. | Юнит-тесты на `applyFilters`: вложенные AND/OR, все операторы, динамические даты, `__me__`. | M |
| 2 | `supabase/functions/_shared/htmlFormatting.ts` (`htmlToTelegramHtml`) | 🔴 | **Нет тестов.** Конвертер HTML→Telegram, используют 4 send-функции. Именно он 17 дней ломал нумерованные списки (`<ol start>` → 1-3) в реальных сообщениях клиентам (ledger 06-11). Фронтовый `messengerHtml.ts` покрыт (15 тестов), а исходящее в канал — нет. | Баг форматирования уходит прямо клиенту в Telegram/Business — видят кривое сообщение. Уже было. | Тесты на конвертер: списки (в т.ч. `start`), blockquote, вложенность, спецсимволы. | M |
| 3 | `src/lib/sidebarSettings.ts` (`reorderWithinZones:458`, `normalizeSidebarSlots:363`) | 🟠 | **Нет тестов** у персист-нормализатора. `reorderWithinZones` имеет отдельный раздел в gotchas (не сортирует, нумерует по текущему порядку — уже ломалось), зовут 5 мест. `sidebarDnd.test.ts` тестирует только DnD-хелперы, не нормализатор. | Баг → у клиента развалится сайдбар / дубли слотов. | Тесты на `reorderWithinZones`/`normalizeSidebarSlots` (порядок, зоны, вложенность папок). | S-M |
| 4 | `.claude/rules/infrastructure.md` раздел «Локальная разработка» + `package.json` | 🟠 | Онбординг неполон: (а) шаг `cp .env.example .env.local` есть только для mtproto, не для основного приложения; (б) в таблице «Окружение» и в `.env.example` нет `NEXT_PUBLIC_MODULE_SUPABASE_URL/_ANON_KEY` (реально читаются в `residence/moduleClient.ts`, их отсутствие = прод-инцидент 06-15); (в) версия Node не зафиксирована (`engines` нет; Dockerfile на node:22, а доки говорят «Node 20» — реальная 22). | Новый разработчик (при росте команды/продаже) не поднимет проект без личной помощи владельца — прямой барьер передачи. | Дописать шаг копирования env, добавить MODULE-переменные в таблицу и `.env.example`, зафиксировать `"engines": {"node": "22"}`. | S |
| 5 | ~12 мёртвых экспортов | 🟡 | Подтверждено grep по имени = 0: `knowledgeBaseService.ts` CRUD (createArticle/updateArticle/deleteArticle/updateGroup/deleteGroup/setArticleGroups — мутации делаются инлайном в `useKnowledgeArticleMutations.ts`), `knowledgeSearchService.searchKnowledgeSources`, `formKitService.updateFormKit`, `sidebarSettings` (linkIdFromSlotId/groupSlots/TOPBAR_SOFT_LIMIT), `EMAIL_SOURCES`, `ACTIVE_NAV_ITEM_HOVER_BG_CLASS`. Файлы-контейнеры (knowledgeBaseService) живые, мёртвые только отдельные экспорты. | Мусор, вводит в заблуждение при чтении. Низкий риск. | Удалить подтверждённо мёртвые экспорты. | S |
| 6 | `src/utils/format/externalHref.ts:12` (`resolveExternalHref`) | 🟡 | Добавлен только что (коммит `9c4e8e6`), но call-site в обработчике клика **не подключён** (0 использований). | Незавершённая фича — хелпер есть, применения нет. Не удалять вслепую (спросить автора). | Подключить или удалить, по решению автора. | S |
| 7 | `as unknown as` — 306 вхождений | 🟡 | Из них **179 в тестах** (моки Supabase — легитимно), остальное — сервис-слой (типизация RPC-результатов) + документированный `supabaseJson.ts`. Не маскировка TS-ошибок (`@ts-ignore` = 0). | Не проблема сейчас; но паттерн «касты для Supabase-типов» разрастается. | Оставить; при регенерации database.ts часть уйдёт. | — |
| 8 | digest-промпт в 2 файлах | 🟡 | `lib/digestDefaults.ts` и `generate-project-digest/index.ts` — текст **идентичен посимвольно** сейчас, но синхронизация ручная (задокументировано в gotchas). | Расхождение при будущей правке одного из двух. | Оставить как есть (задокументировано); при желании — вынести в общий источник. | — |

## Проверено — проблем нет

- **Лицензии**: 305 MIT + Apache/ISC/BSD/0BSD, 1 LGPL-3.0 (динамическая линковка — ок для коммерции), дуальные MIT/GPL и MPL/Apache (выбираем permissive). **Чистого GPL/AGPL нет** — продаже ничто не мешает. UNLICENSED = собственный package.json проекта.
- **Зависимости**: неиспользуемых нет (depcheck); «missing» — это Deno-импорты edge-функций (ложные).
- **Гигиена**: 0 TODO/FIXME, 0 `@ts-ignore` (1 `@ts-expect-error`), 7 явных `any`, нет `.bak`/`old_`/`.orig`.
- **Слои**: нарушений направления зависимостей на рантайме нет; 15 импортов из `components` в нижние слои — только `import type` (мягкий остаток документированного T1).
- **Дубли значений**: палитра акцентов синхронна по 17 слагам в 4 источниках (`threadConstants` импортирует union, не дублирует; `Record<ThreadAccentColor>` ловит расхождение tsc); digest-промпт идентичен.
- **Покрыто тестами (критичное)**: права (`useProjectPermissions`/`useWorkspacePermissions`), inbox-классификаторы (`inboxUnread`), access-фильтры (`useAccessibleThreadIds`/`useFilteredInbox`), query keys (ключевые модули + `invalidateAfterThreadMove`).
- Большие файлы (`SidebarEditorCanvas` 1092, `MessengerTabContent` 787, и т.д.) — оркестраторы, по списку false-positives не репортятся.

## Главное для передачи команде

1. **Три набора тестов до продажи** (дёшево, высокий риск тихой поломки): `filterEngine.applyFilters`, `htmlToTelegramHtml`, `reorderWithinZones`/`normalizeSidebarSlots`.
2. **Починить онбординг-доку** (#4) — иначе новый разработчик не поднимет проект без помощи владельца.
3. Архитектура здорова: дублей-рассинхронов нет, права/доступы покрыты, мёртвого кода мало.

## Не проверено

- Полный обход всех экспортов на мёртвость (проверены top-кандидаты; barrel-реэкспорты требуют grep по имени, сделан для перечисленных).
- Покрытие тестами каждого из 16 query-key модулей (проверены ключевые).
