# Сайдбар: последовательная анимация вкладок, плотность списка

**Дата:** 2026-04-05
**Тип:** ui, polish
**Статус:** completed

---

## Что сделано

### Последовательная анимация раскрытия вкладок
- `src/app/globals.css` — вкладки старого проекта теперь сначала сворачиваются (180мс), и только потом разворачиваются вкладки нового (200мс с задержкой 200мс)
- Раньше обе анимации шли одновременно — визуально лагало из-за параллельных reflow-ов. Теперь переход чёткий и последовательный

### Уменьшены отступы между проектами в списке
- `src/components/WorkspaceSidebar/ProjectListItem.tsx` — `p-1` → `px-1 py-0` у обёртки проекта
- После добавления рамки вокруг активного проекта с вкладками вертикальный padding делал список слишком разреженным. Теперь горизонтальный padding 4px сохраняется (для рамки), вертикальный убран — список вернулся к прежней плотности
