# 2026-07-21 — Меню «Создать» на вкладке задач: ширина +30%

Выпадающее меню split-кнопки «Создать» (Группа / Задача / Чат / Email + список
шаблонов тредов проекта) было узким — длинные названия шаблонов обрезались
(`truncate`). Ширина увеличена `w-56` (224px) → `w-[18.2rem]` (291px), ровно +30%.

Чистая CSS-правка (`DropdownMenuContent`/`PopoverContent` в
`CreateThreadButtonGroup.tsx`), логика не тронута.
