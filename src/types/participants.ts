/**
 * Минимальная форма участника для отрисовки аватарок внахлёст.
 * Определение вынесено сюда (нижний слой), чтобы хуки/утилиты не импортировали
 * вверх из `components/participants/ParticipantAvatars` (который реэкспортирует тип).
 */

export type AvatarParticipant = {
  id: string
  name: string
  last_name: string | null
  avatar_url: string | null
}

/**
 * Динамический исполнитель «Создатель задачи»: не человек, а правило подстановки
 * (кто создаёт — тот и исполнитель). В списках исполнителей идёт отдельной
 * группой; в таблицы исполнителей не пишется — там FK на конкретного участника,
 * поэтому хранится флагом на источнике (thread_templates.assign_to_creator).
 *
 * Живёт здесь, а не в types/threadTemplate: общий пикер исполнителей не должен
 * зависеть от домена шаблонов.
 */
export const CREATOR_ASSIGNEE_ID = '__creator__'
