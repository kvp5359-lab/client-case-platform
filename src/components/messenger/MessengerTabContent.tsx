"use client"

/**
 * Stub — будет заменён на этапе 8 (мессенджер)
 */

export function MessengerTabContent(props: Record<string, unknown>) {
  return (
    <div className="p-4 text-muted-foreground text-sm">
      Мессенджер будет доступен после переноса (этап 8)
    </div>
  )
}
