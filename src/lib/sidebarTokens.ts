/**
 * Дизайн-токены для подсветки активного/выбранного элемента в навигации
 * воркспейса. Единый source of truth — меняешь здесь, меняется во всех
 * местах, которые импортируют (сайдбар с проектами, строка задачи,
 * подсветка вкладки в TaskPanel и т.д.).
 *
 * ВНИМАНИЕ: Tailwind purge видит классы только если они присутствуют
 * как строковые литералы в файлах, попадающих в `content` tailwind.config.
 * Здесь они есть — class names не разрывать на части и не строить
 * динамически.
 */

/** Фон активного элемента навигации (активный проект, активная вкладка
 *  в списке задач). */
export const ACTIVE_NAV_ITEM_BG_CLASS = 'bg-gray-200'

/** Hover-вариант — отдельный литерал, потому что Tailwind purge не видит
 *  динамические склейки `hover:${X}`. При смене BG обновляй обе. */
export const ACTIVE_NAV_ITEM_HOVER_BG_CLASS = 'hover:bg-gray-200'

/** Цвет текста активного элемента навигации (вместе с фоном). */
export const ACTIVE_NAV_ITEM_TEXT_CLASS = 'text-gray-900'

/** Полный класс активного элемента (фон + цвет текста). */
export const ACTIVE_NAV_ITEM_CLASS = `${ACTIVE_NAV_ITEM_BG_CLASS} ${ACTIVE_NAV_ITEM_TEXT_CLASS}`
