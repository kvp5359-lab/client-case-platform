/**
 * Лимиты размера файлов по контекстам загрузки.
 *
 * Карантинные мессенджер-лимиты (TG/Wazzup) живут внутри messenger/ —
 * там 20 МБ для обмена с клиентом. Здесь — лимиты не-карантинных
 * экранов (анкеты, шаблоны, документы проекта).
 */

/** Лимит загрузки для anti-fill анкет (PDF/JPG/PNG). 10 МБ. */
export const FORM_AUTOFILL_MAX_BYTES = 10 * 1024 * 1024
